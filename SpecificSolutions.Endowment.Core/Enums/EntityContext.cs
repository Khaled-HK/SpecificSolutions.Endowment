namespace SpecificSolutions.Endowment.Core.Enums
{
    public enum EntityContext
    {

        //[Display(Name = nameof(Phrases.All), ResourceType = typeof(Phrases))]
        All,
        UserGroup,
        User,
        Login,
        Account,
    }
}
